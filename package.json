{
  "name": "sudoku-cf",
  "version": "1.0.0",
  "description": "A free online Sudoku puzzle game deployed on Cloudflare Pages",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "dev": "vitest --watch"
  },
  "devDependencies": {
    "@vitest/ui": "^1.6.0",
    "@vitest/coverage-v8": "^1.6.0",
    "vitest": "^1.6.0",
    "jsdom": "^24.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
